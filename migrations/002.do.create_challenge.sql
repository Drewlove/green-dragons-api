DROP TABLE IF EXISTS CHALLENGE CASCADE; 

CREATE TABLE challenge(
    challenge_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    challenge_name TEXT NOT NULL,
    challenge_description TEXT NOT NULL,
    units TEXT NOT NULL
);
